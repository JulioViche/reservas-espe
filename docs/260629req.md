# Requerimientos

En el microservicio de users, añadir JWT para 

Client:

- login
- zones (R)
- spaces (R)
- vehicles (R, own)
- users (RU, own)
- persons (RU, own)

Admin:

- zones (CRUD)
- spaces (CRUD)
- vehicles (CRUD)
- persons (RUD)
- users (RUD)
- roles (CRUD)
- assignments (CRUD)

Collector:

- ...

Root:

- physical delete